{
  "name": "dm-customer-11",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "start": "npm run build && node server.js.bak"
  },
  "devDependencies": {
    "missing": "*"
  }
}
